مستند نیازمندی‌های محصول

بازار فرش ایران

نسخه ۱ — فروشگاه تک‌فروشنده

| نسخه سند             | 0.3                                                 |
|----------------------|-----------------------------------------------------|
| نسخه محصول           | V1                                                  |
| وضعیت داکیومنت       | پیش‌نویس اصلاح‌شده پس از بازبینی سوم Product Designer |
| مالک                 | Product Manager                                     |
| طراح                 | Product Designer — بازبینی انجام شده                |
| برنامه‌نویسان         | TBD                                                 |
| تضمین کیفیت          | TBD                                                 |
| تأییدکنندگان         | TBD                                                 |
| تاریخ تهیه           | ۱۴۰۵/۰۶/۰۶                                          |
| تاریخ تأیید و انتشار | TBD                                                 |

| این سند نسخه 0.3 PRD پس از بازبینی سوم Product Designer است. تصمیم‌های قطعی الحاقیه طراحی در متن اصلی ادغام شده‌اند. در این بازبینی، مبنای تبدیل قیمت انگلیسی به «نرخ فروش دلار بازار آزاد» نهایی شده و مبلغ USD به‌صورت تقریبی نمایش داده می‌شود. انتخاب تأمین‌کننده نرخ به تیم فنی واگذار شده و باید پیش از اتصال واقعی سرویس انجام شود. موضوع غیرمسدودکننده باقی‌مانده در پیوست ۱۶.۲ مشخص شده است. |
|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

۱. اهداف

۱.۱ مسئله

در بازار فعلی فرش، بستر یکپارچه‌ای وجود ندارد که فروشنده بتواند موجودی فرش‌های خود را با تصویر، مشخصات ساختاریافته و قیمت به نمایش بگذارد و مشتری نیز بتواند پیش از مراجعه حضوری، گزینه‌های مختلف را به‌سادگی پیدا و مقایسه کند.

فرآیند رایج فعلی عمدتاً به معرفی دهان‌به‌دهان، تماس تلفنی و ارسال عکس و اطلاعات در پیام‌رسان‌هایی مانند واتساپ یا بله متکی است. مشتری ابتدا فروشنده یا محصول را پیدا می‌کند، برای قیمت تماس می‌گیرد، تصاویر را درخواست می‌کند و در صورت مناسب بودن، برای مشاهده حضوری مراجعه می‌کند. این مسیر برای مشتری پراکنده و زمان‌بر است و برای فروشنده نیز شامل پاسخ‌گویی و ارسال تکراری اطلاعات و تصاویر به مشتریان مختلف می‌شود.

بر اساس بررسی میدانی انجام‌شده با چند فعال بازار فرش، مشاهده حضوری فرش پیش از خرید بخش مهمی از رفتار خرید این بازار است و در نسخه اول قرار نیست حذف شود. بنابراین مسئله‌ای که این محصول حل می‌کند «خرید آنلاین فرش» نیست؛ بلکه ساده‌سازی مرحله کشف، ارزیابی اولیه، اطلاع از قیمت و رسیدن به فروشنده پیش از مراجعه حضوری است.

۱.۲ ویژن محصول

ایجاد بستری که مشتری بتواند فرش مناسب را سریع‌تر پیدا کند، تصاویر و مشخصات و قیمت آن را پیش از مراجعه حضوری ببیند و سپس مستقیماً با فروشنده ارتباط بگیرد؛ و در مقابل، فروشنده بتواند فرش‌های خود را یک‌بار به‌صورت ساختاریافته منتشر کند و به جای ارسال تکراری اطلاعات، مشتریان آماده‌تر و باکیفیت‌تری جذب کند.

۱.۳ هدف نسخه ۱

نسخه اول یک Market عمومی دو‌زبانه (فارسی/انگلیسی) برای مشاهده و جستجوی فرش‌های یک فروشگاه و یک Admin Panel فارسی برای مدیریت همان محصولات فراهم می‌کند. فروشگاه پایلوت این نسخه «فروشگاه فرش ایران» در تهران است. اولویت محتوایی و بصری Market با فرش دستباف است، اما فرش ماشینی نیز در V1 پشتیبانی می‌شود.

| ورود به Market | ←   | جستجو و انتخاب | ←   | مشاهده جزئیات و قیمت | ←   | تماس با فروشنده | ←   | مراجعه حضوری |
|----------------|-----|----------------|-----|----------------------|-----|-----------------|-----|--------------|

| ترتیب بالا از راست به چپ خوانده می‌شود. خرید و پرداخت در خارج از محصول و به‌صورت حضوری انجام می‌شود. |
|---------------------------------------------------------------------------------------------------|

۱.۴ جامعه هدف و پرسوناها

مشتری هدف‌دار: کاربری که حدوداً یا دقیقاً می‌داند چه نوع فرشی می‌خواهد و از Search، Filter و Sort برای رسیدن سریع به گزینه مناسب و مقایسه قیمت‌ها استفاده می‌کند.

مشتری اکتشافی: کاربری که هنوز انتخاب نهایی ندارد و با مرور Market، تصاویر، مشخصات و قیمت‌ها به انتخاب می‌رسد.

فروشنده / Admin: مدیر فروشگاه فرش ایران که محصولات، تصاویر، قیمت و وضعیت موجودی را مدیریت می‌کند و عملکرد Market را از طریق Dashboard می‌بیند.

زبان تجربه: مشتری می‌تواند بخش عمومی را به فارسی یا انگلیسی استفاده کند؛ Admin Panel فقط فارسی است و فروشنده محتوای فارسی و انگلیسی محصول را داخل همان پنل وارد می‌کند.

۱.۵ ارزش پیشنهادی

• برای مشتری: کاهش زمان پیدا کردن گزینه مناسب، امکان مشاهده اولیه عکس و مشخصات، مشاهده قیمت بدون استعلام تلفنی، مقایسه گزینه‌ها و دسترسی مستقیم به فروشنده.

• برای فروشنده: ایجاد ویترین دائمی و ساختاریافته، کاهش ارسال تکراری عکس و مشخصات، کاهش تماس‌های صرفاً استعلام قیمت، افزایش دیده‌شدن محصولات و افزایش احتمال جذب مشتری مناسب.

۱.۶ KPI و معیار موفقیت نسخه ۱

KPI اصلی — Contact Conversion Rate: نسبت Sessionهای یکتایی که پس از مشاهده Product Detail همان محصول، رویداد contact_clicked داشته‌اند به Sessionهای یکتایی که Product Detail را مشاهده کرده‌اند. محاسبه بر اساس Session یکتا است، نه تعداد خام کلیک‌ها.

هدف عددی برای این KPI در زمان نگارش PRD تعیین نشده است؛ نسخه اول باید Baseline واقعی ایجاد کند تا هدف عددی نسخه‌های بعد بر اساس داده تعیین شود.

فرمول: Contact Conversion Rate = 100 × unique qualifying sessions / unique product-view sessions. اگر مخرج صفر باشد، مقدار «داده کافی نیست» نمایش داده می‌شود، نه ۰٪.

• Supporting Metrics: تعداد بازدید Market، تعداد Product View، تعداد Contact Click، تعداد Phone Call Click، نرخ Market → Product Detail، استفاده از Search و Filter، محصولات پربازدید، محصولات با بیشترین Contact و Search Queryهای پرتکرار.

۱.۷ خارج از Scope مسئله در V1

• خرید و پرداخت آنلاین

• ثبت سفارش

• حساب کاربری مشتری

• Messaging بین مشتری و فروشنده

• چندفروشندگی (Multi-vendor)

• Subscription

• Super Admin

• مدیریت اعضای فروشگاه

• Draft محصول

• OTP

• ویدئو

• Share Product

• Similar Products

• Discount

• Favorite / Wishlist

• Rating & Review

• جفت/ست چندتخته‌ای به‌عنوان یک محصول؛ هر رکورد فقط یک تخته فرش است.

• فرش‌های گرد، بیضی یا شکل‌های غیرمستطیلی.

• ترجمه خودکار محتوا.

• ارسال بین‌المللی، پرداخت ارزی یا تضمین خرید صرفاً به دلیل وجود نسخه انگلیسی و نمایش USD.

۲. راه‌حل (Solution)

راه‌حل نسخه اول از دو Entry Point مستقل تشکیل می‌شود: Market عمومی دو‌زبانه برای مشتری و Admin Panel فارسی برای فروشنده. Landing Page جداگانه در V1 وجود ندارد.

| بخش         | کاربر                  | هدف                                                                      |
|-------------|------------------------|--------------------------------------------------------------------------|
| Market      | مشتری عمومی بدون Login | جستجو، فیلتر، مرور، مشاهده جزئیات و قیمت و رسیدن به اطلاعات تماس فروشنده |
| Admin Panel | یک Admin برای فروشگاه  | ورود با موبایل و رمز عبور، مدیریت محصولات و مشاهده Dashboard             |

Market باید برای دو رفتار اصلی پشتیبانی شود: کاربر هدف‌دار که با Search و Filter به دنبال محصول مشخص است و کاربر اکتشافی که با مرور محصولات به انتخاب می‌رسد.

محصول نقش کاتالوگ/Marketplace برای مرحله پیش از خرید را دارد. تماس تلفنی و مراجعه حضوری بخشی از Journey است، اما نتیجه تماس و خرید نهایی در V1 داخل سیستم ثبت نمی‌شود.

در نسخه انگلیسی همان موجودی و همان فروشگاه تهران نمایش داده می‌شود و قیمت پایه تومانی محصولات با «نرخ فروش دلار بازار آزاد» که به‌صورت خودکار و حداقل روزانه به‌روزرسانی می‌شود، به USD تبدیل و به‌صورت تقریبی نمایش داده می‌شود. نمایش USD به معنی اضافه‌شدن Checkout، پرداخت ارزی، تضمین قیمت نهایی معامله یا ارسال بین‌المللی نیست.

۳. دیاگرام‌های رفتاری (Behavioral Diagrams)

۳.۱ Customer Discovery Flow

| Market | ←   | جستجو / فیلتر / مرتب‌سازی | ←   | انتخاب محصول | ←   | جزئیات محصول | ←   | تماس |
|--------|-----|--------------------------|-----|--------------|-----|--------------|-----|------|

کاربر می‌تواند بدون استفاده از Search نیز مستقیماً از طریق Browse به Product Detail برسد. همه مراحل لازم برای ساخت Funnel باید Event Tracking مناسب داشته باشند.

۳.۲ Admin Product Management Flow

| ورود | ←   | لیست محصولات | ←   | افزودن محصول | ←   | فرم + تصاویر | ←   | ثبت محصول | ←   | انتشار در Market |
|------|-----|--------------|-----|--------------|-----|--------------|-----|-----------|-----|------------------|

Draft در V1 وجود ندارد. محصول پس از تکمیل معتبر فیلدهای اجباری و ثبت، منتشر می‌شود. Admin می‌تواند پس از انتشار تمام مشخصات محصول را ویرایش کند.

۳.۳ Product Status Flow

| Available | ←   | Reserved | ←   | Sold |
|-----------|-----|----------|-----|------|

• Available: در Listing پیش‌فرض Market نمایش داده می‌شود و تماس مجاز است.

• Reserved: در Listing نمایش داده می‌شود، وضعیت رزرو باید واضح باشد و تماس همچنان مجاز است.

• Sold: از Listing پیش‌فرض حذف می‌شود، اما Public URL و Product Detail باقی می‌ماند و تماس همچنان مجاز است.

۳.۴ Analytics Data Flow

| تعامل کاربر | ←   | Session ناشناس | ←   | ذخیره Event | ←   | تجمیع Metrics | ←   | Dashboard |
|-------------|-----|----------------|-----|-------------|-----|---------------|-----|-----------|

Analytics مشتری بدون Login و بر اساس Anonymous ID / Session انجام می‌شود. برای Analytics این نسخه اطلاعات هویتی مشتری ذخیره نمی‌شود.

۴. امکانات و ویژگی‌ها

۴.۱ Market عمومی

Market صفحه عمومی دو‌زبانه مشاهده محصولات است و نیاز به ثبت‌نام یا Login ندارد. مسیر عمومی مستقیماً با جستجو، فیلتر و محصولات شروع می‌شود و اولویت محتوایی/بصری با فرش دستباف است، بدون آنکه فرش ماشینی مخفی شود.

Acceptance Criteria:

• کاربر بدون Authentication بتواند Market را مشاهده کند.

• محصولات Available و Reserved در Listing پیش‌فرض قابل مشاهده باشند.

• محصولات Sold در Listing پیش‌فرض نمایش داده نشوند.

• ساختار Listing و Product Card باید امکان تصمیم‌گیری سریع بر اساس اطلاعات کلیدی را فراهم کند؛ اولویت و چینش اطلاعات توسط Product Designer تعیین می‌شود.

نبایدها: Landing Page جداگانه، Account مشتری، سفارش آنلاین و پرداخت در این نسخه وجود ندارد.

۴.۲ Search، Filter و Sort

Search در V1 ساده و Keyword-based است و قرار نیست Semantic Search، NLP یا ترجمه خودکار Query باشد. جستجو باید روی مقادیر فارسی و انگلیسی موجود کار کند. در کنار آن Filter قوی و Multi-select وجود دارد.

Search Scope: عنوان، توضیحات متنی و نام داده‌های ساختاریافته قابل جستجو باشند؛ از جمله شهر بافت/تولید، نوع بافت، جنس، رنگ، طرح و برند/کارخانه در صورت وجود.

Filterهای پایه: بازه قیمت، طول/عرض یا ابعاد مشتق‌شده، نوع فرش (دستباف/ماشینی)، شهر بافت/تولید، نوع بافت، جنس، رنگ، طرح و نو/دست‌دوم.

Filterهای تخصصی: فقط وقتی نوع مرتبط به‌تنهایی انتخاب شده باشد نمایش داده شوند؛ برای دستباف رج و قدمت، و برای ماشینی شانه، تراکم و برند/کارخانه. با تغییر نوع، Filter تخصصی ناسازگار باید پاک شود و این تغییر برای کاربر واضح باشد.

Sort: کاربر بتواند Sort را تغییر دهد؛ حالت پیش‌فرض «جدیدترین» است. حداقل Sort قیمت از کم به زیاد و زیاد به کم نیز پشتیبانی شود.

• Filterهایی که مجموعه چندگزینه‌ای دارند باید Multi-select باشند. چند گزینه داخل یک گروه با منطق OR و گروه‌های مختلف با منطق AND ترکیب می‌شوند.

• Search، Filter، Sort و Page در Query Params URL نگه‌داری شوند تا Refresh، Back و بازکردن URL مشترک همان State را بازسازی کند. حدود قیمت در URL به واحد پایه (تومان) نگه‌داری شوند تا تغییر زبان دامنه اقتصادی انتخاب‌شده را عوض نکند.

• بازه قیمت باید ورودی حداقل و حداکثر با واحد واضح داشته باشد؛ در صورت استفاده از Slider، Slider تنها روش ورود مبلغ نباشد. Listing در مبنای طراحی V1 صفحه‌بندی (Pagination) می‌شود تا بازگشت به نتایج و بازسازی State قابل پیش‌بینی باشد.

• Filterهای فعال، حذف تک‌فیلتر، پاک‌کردن همه و تعداد نتایج باید قابل مشاهده باشند.

• حالت بدون نتیجه باید امکان اصلاح Query یا پاک‌کردن Filterها را نشان دهد.

• بازگشت از Product Detail باید Query، Filter، Sort، Page و تا حد امکان موقعیت مرور را حفظ کند.

۴.۳ Product Detail

هر محصول Public URL مستقل و غیرقابل حدس دارد و صفحه جزئیات در هر دو زبان باید اطلاعات لازم برای ارزیابی اولیه فرش را نمایش دهد. تغییر زبان باید همان Product Public ID را حفظ کند.

• تصاویر و Cover Image

• عنوان

• قیمت

• در فارسی قیمت پایه به تومان و در انگلیسی مبلغ تقریبی محاسبه‌شده به USD نمایش داده شود؛ تبدیل بر اساس نرخ فروش دلار بازار آزاد انجام شود و هر دو نمای Card و Detail از یک نسخه معتبر نرخ استفاده کنند.

• وضعیت موجودی

• مشخصات ساختاریافته متناسب با نوع فرش

• Description آزاد

• نام فروشگاه، شهر فروشگاه و شماره موبایل

• آدرس فروشگاه در صورت موجود بودن

CTA اصلی صفحه «تماس با فروشنده» است و در Available، Reserved و Sold همیشه فعال می‌ماند. جریان تماس باید دو مرحله قابل اندازه‌گیری داشته باشد: ۱) بازکردن اطلاعات فروشگاه و ثبت contact_clicked؛ ۲) انتخاب «شروع تماس» و اجرای tel: همراه ثبت phone_call_clicked. بازکردن اطلاعات تماس نباید خودکار تماس را شروع کند.

نبایدها: Similar Products و Share Product در V1 وجود ندارند.

• برای محصول Sold پیام روشن نمایش داده شود که فرش فروخته شده است اما تماس برای اطلاعات بیشتر همچنان ممکن است.

• محصول Soft Deleted نباید Public Detail قابل دسترس داشته باشد.

۴.۴ اطلاعات محصول و Data Model محصولی

هر رکورد محصول مربوط به یک تخته فرش است و جفت/ست در V1 نداریم. تمام فیلدهای عمومی زیر برای ثبت محصول اجباری هستند؛ Description اختیاری است. جنس و رنگ چندانتخابی‌اند. ابعاد با طول و عرض جداگانه ثبت می‌شوند و فقط شکل مستطیلی پشتیبانی می‌شود. فیلدهای تخصصی بر اساس نوع فرش اجباری می‌شوند.

| فیلد                        | وضعیت                                 | توضیح                                                                                                                                                                                                     |
|-----------------------------|---------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| عنوان فارسی / انگلیسی       | فارسی اجباری؛ سیاست الزام انگلیسی باز | فروشنده محتوای دو‌زبانه را وارد می‌کند. سیاست نهایی الزام عنوان انگلیسی در پیوست ۱۶.۲ مشخص شده است.                                                                                                         |
| قیمت                        | اجباری                                | قیمت پایه هر تخته به تومان ثبت می‌شود؛ «تماس بگیرید» مجاز نیست. مبلغ انگلیسی به‌صورت تقریبی و بر اساس نرخ فروش دلار بازار آزاد که حداقل روزانه به‌روزرسانی می‌شود محاسبه می‌شود و فیلد مستقل قابل ویرایش نیست. |
| طول و عرض                   | اجباری                                | دو مقدار مثبت جداگانه برای فرش مستطیلی؛ اندازه‌های غیررایج مستطیلی مجازند. شکل گرد/بیضی خارج از V1 است. واحد ثبت داخلی پیش از پیاده‌سازی نهایی می‌شود؛ پیشنهاد طراحی سانتی‌متر است.                           |
| شهر بافت/تولید              | اجباری                                | از Reference Data.                                                                                                                                                                                        |
| نوع فرش                     | اجباری                                | دستباف یا ماشینی.                                                                                                                                                                                         |
| نوع بافت                    | اجباری                                | از Reference Data.                                                                                                                                                                                        |
| جنس                         | اجباری                                | از Reference Data؛ یک یا چند گزینه، حداقل یک انتخاب. درصد ترکیب در V1 ثبت نمی‌شود.                                                                                                                         |
| رنگ                         | اجباری                                | از Reference Data؛ یک یا چند گزینه، حداقل یک انتخاب؛ برچسب نوشتاری رنگ نمایش داده شود.                                                                                                                    |
| طرح                         | اجباری                                | از Reference Data.                                                                                                                                                                                        |
| وضعیت محصول                 | اجباری                                | نو یا دست‌دوم.                                                                                                                                                                                             |
| قدمت                        | اجباری                                | برای هر دو نوع فرش اجباری است. پیشنهاد طراحی: عدد صحیح نامنفی بر حسب سال با عنوان «قدمت تقریبی»؛ سیاست دقیق نمایش قبل از پیاده‌سازی نهایی شود.                                                             |
| وضعیت موجودی                | اجباری                                | Available / Reserved / Sold.                                                                                                                                                                              |
| تصاویر                      | اجباری                                | حداقل ۱ و حداکثر ۱۰ تصویر؛ یک Cover الزامی.                                                                                                                                                               |
| Description فارسی / انگلیسی | اختیاری                               | متن آزاد اختیاری. فروشنده امکان ورود هر دو زبان را دارد؛ رفتار نسخه انگلیسی هنگام نبود ترجمه در پیوست ۱۶.۲ باز است.                                                                                       |

دستباف: رج به‌عنوان فیلد تخصصی اجباری نمایش داده می‌شود.

ماشینی: شانه، تراکم و برند/کارخانه به‌عنوان فیلدهای تخصصی اجباری نمایش داده می‌شوند.

| فرم Create/Edit باید Dynamic باشد؛ با انتخاب نوع فرش، فیلدهای تخصصی همان نوع نمایش داده شوند. |
|-----------------------------------------------------------------------------------------------|

۴.۵ تصاویر محصول

• حداقل ۱ و حداکثر ۱۰ تصویر برای هر محصول.

• یک تصویر باید به‌عنوان Cover Image مشخص شود.

• حداکثر حجم هر فایل تصویر ۱۵ مگابایت است.

• ویدئو در V1 پشتیبانی نمی‌شود.

• سیستم باید از سرو مستقیم تصاویر بسیار حجیم در Listing جلوگیری کند و برای تجربه کاربری، نمایش تصاویر را بهینه کند؛ روش فنی به تیم توسعه واگذار می‌شود.

• تصاویر باید Alt Text مناسب داشته باشند؛ در محتوای محصول می‌توان از عنوان/توضیح مرتبط برای تولید متن جایگزین قابل فهم استفاده کرد، بدون ادعای تشخیص خودکار محتوای تصویر.

۴.۶ وضعیت موجودی

• Admin بتواند وضعیت هر محصول را به صورت دستی بین Available، Reserved و Sold تغییر دهد.

• Reserved در Listing باقی بماند و Badge/نمایش واضح وضعیت داشته باشد.

• Sold از Listing پیش‌فرض حذف شود ولی Product Detail و URL باقی بماند.

• تماس با فروشنده در هر سه وضعیت فعال باشد.

۴.۷ تماس با فروشنده

حداقل اطلاعات فروشگاه که مشتری باید در مسیر تماس ببیند عبارت‌اند از نام فروشگاه، شهر فروشگاه و شماره موبایل. آدرس فروشگاه اختیاری است.

• نام فروشگاه V1: فروشگاه فرش ایران.

• شهر فروشگاه V1: تهران.

• شماره موبایل برای ارتباط اجباری است.

• CTA تماس باید روی موبایل امکان آغاز تماس تلفنی را فراهم کند.

• ساختار UI تماس باید دو مرحله مستقل داشته باشد: نمایش پنل اطلاعات فروشگاه = contact_clicked و کلیک «شروع تماس» = phone_call_clicked. این رویدادها نباید به‌عنوان تماس موفق یا فروش گزارش شوند.

۴.۸ ورود Admin

در V1 هر فروشگاه تنها یک Admin دارد. Admin با شماره موبایل و رمز عبور وارد می‌شود.

• Login با Mobile Number + Password.

• Logout باید وجود داشته باشد.

• OTP در V1 وجود ندارد.

• Forgot Password مبتنی بر OTP و سایر روش‌های بازیابی رمز در Scope این نسخه تعریف نشده‌اند.

• جزئیات سیاست Session، محدودیت تلاش ناموفق و Rate Limit در طراحی امنیتی تیم فنی مشخص شود.

۴.۹ مدیریت محصولات در Admin

• مشاهده لیست محصولات.

• Search و Filter در لیست محصولات Admin.

• Create محصول.

• Edit تمام مشخصات محصول پس از انتشار.

• تغییر دستی وضعیت موجودی.

• حذف محصول به صورت Soft Delete.

• مشاهده حداقل تعداد Product View و Contact Click هر محصول در Product List.

Draft در V1 وجود ندارد. محصول تنها زمانی قابل ثبت و انتشار است که تمام فیلدهای اجباری متناسب با نوع آن معتبر باشند.

• فرم Create/Edit در رابط فارسی، فیلدهای عنوان و Description فارسی و انگلیسی را با برچسب جداگانه دریافت کند؛ ورودی انگلیسی LTR باشد.

• جنس و رنگ در فرم Multi-select باشند. هنگام تغییر نوع دستباف/ماشینی، حذف داده‌های تخصصی ناسازگار بدون هشدار انجام نشود.

• قیمت و وضعیت موجودی باید از مسیر مدیریت محصول به‌سادگی قابل ویرایش باشند و نتیجه موفق/ناموفق تغییر بازخورد صریح داشته باشد.

• خروج از فرم دارای تغییر ذخیره‌نشده هشدار داشته باشد؛ این رفتار به معنی Draft نیست.

• خطای Validation کنار فیلد مربوط نمایش داده شود و Upload نامعتبر هر تصویر به‌صورت مستقل مشخص شود.

• حذف محصول نیازمند Confirm واضح باشد و به Admin اعلام کند محصول از نمایش عمومی خارج می‌شود ولی History و Analytics حفظ می‌شود؛ Restore/Trash در V1 نداریم.

۴.۱۰ Soft Delete

حذف محصول باید Soft Delete باشد تا تاریخچه و Analytics آن از بین نرود. محصول Soft Deleted نباید در Market یا Public Product Detail قابل دسترس باشد، اما رکورد آن برای نیازهای داخلی و گزارش‌گیری حفظ می‌شود.

۴.۱۱ اطلاعات فروشگاه

اطلاعات هویتی اصلی فروشگاه در V1 از قبل در سیستم ثبت می‌شوند و Admin امکان ویرایش آزادانه آن‌ها را ندارد. این محدودیت برای حفظ یکپارچگی هویت فروشگاه و آماده‌سازی معماری نسخه‌های بعدی است.

| فیلد              | وضعیت در V1                             |
|-------------------|-----------------------------------------|
| نام فروشگاه       | اجباری و غیرقابل ویرایش توسط Admin      |
| شهر               | اجباری و غیرقابل ویرایش توسط Admin      |
| شماره موبایل تماس | اجباری و غیرقابل ویرایش آزادانه         |
| آدرس فروشگاه      | اختیاری؛ نحوه نمایش با Product Designer |

۴.۱۲ Reference Data

گزینه‌های محدود مانند نوع بافت، طرح، جنس، رنگ، شهر و سایر داده‌های مرجع در V1 از قبل در Database Seed می‌شوند. هر گزینه Reference Data باید عنوان فارسی و انگلیسی داشته باشد و شناسه آن مستقل از زبان باشد. فروشنده امکان افزودن، حذف یا ویرایش این گزینه‌ها را ندارد. مدیریت Reference Data می‌تواند در نسخه‌های بعد در اختیار Super Admin قرار گیرد.

۴.۱۳ Dashboard فروشنده

Admin Panel دارای Dashboard است. Dashboard باید حداقل تصویری از وضعیت موجودی و عملکرد Market ارائه دهد.

• تعداد کل محصولات و تفکیک Available / Reserved / Sold.

• تعداد Product View.

• تعداد Contact Click و Phone Call Click.

• Contact Conversion Rate.

• محصولات پربازدید.

• محصولات با بیشترین Contact.

• Search Queryهای پرتکرار یا دسترسی به گزارش آن‌ها.

مبنای طراحی Dashboard: خلاصه موجودی جاری مستقل از بازه زمانی نمایش داده شود؛ شاخص‌های عملکردی با بازه پیش‌فرض ۳۰ روز اخیر و امکان انتخاب ۷ روز و بازه دلخواه ارائه شوند. Visualization و جزئیات چیدمان توسط Product Designer تعیین می‌شود، اما موجودی جاری نباید با Metrics بازه زمانی مخلوط شود.

۴.۱۴ Analytics و Event Tracking

برای ساخت Funnel و سنجش KPI، Event Tracking بخشی از Requirement نسخه اول است. Eventها باید در سطح Anonymous Session ثبت شوند و اطلاعات هویتی مشتری برای Analytics ذخیره نشود. زبان رابط (fa/en) به‌عنوان Property رویداد ثبت شود.

| Event                | Trigger                        | Properties کلیدی                                          |
|----------------------|--------------------------------|-----------------------------------------------------------|
| market_viewed        | ورود/مشاهده Market             | session_id, timestamp, language                           |
| search_performed     | اجرای Search                   | query, session_id, timestamp, language                    |
| filter_applied       | اعمال یا تغییر Filter          | filter_name, filter_value(s), session_id, language        |
| sort_changed         | تغییر Sort                     | sort_value, session_id, language                          |
| product_card_clicked | انتخاب یک محصول از Listing     | product_public_id, session_id, language                   |
| product_viewed       | مشاهده Product Detail          | product_public_id, inventory_status, session_id, language |
| contact_clicked      | نشان‌دادن قصد ارتباط با فروشنده | product_public_id, session_id, language                   |
| phone_call_clicked   | اقدام برای شروع تماس تلفنی     | product_public_id, session_id, language                   |
| gallery_interacted   | تعامل با Gallery؛ Supporting   | product_public_id, session_id, language                   |

| phone_call_clicked به معنی تماس موفق یا فروش نیست. خرید نهایی خارج از محصول انجام می‌شود و V1 نمی‌تواند Conversion واقعی به خرید را اندازه‌گیری کند. |
|---------------------------------------------------------------------------------------------------------------------------------------------------|

• مخرج Contact Conversion Rate: Sessionهای یکتایی که در بازه گزارش حداقل یک Product Detail را دیده‌اند.

• صورت: زیرمجموعه همان Sessionها که در همان Session و برای همان Product پس از مشاهده Detail رویداد contact_clicked داشته‌اند.

• در KPI کلی فروشگاه هر Session فقط یک بار و در KPI محصول یکتایی در محدوده همان Product محاسبه می‌شود.

• هر دو Event مشاهده و contact_clicked باید در بازه گزارش قرار داشته باشند. طول Session و پایان آن باید پیش از اجرا توسط تیم فنی تعریف و مستند شود.

• تعداد خام View/Click باید جدا از تعداد Session یکتا و با Label دقیق گزارش شود.

۴.۱۵ شناسه محصول و Public URL

• هر محصول یک ID داخلی برای Database دارد.

• ID داخلی ترتیبی/قابل حدس نباید در Public URL یا API عمومی به عنوان شناسه اصلی Exposure داده شود.

• برای Public Identifier از UUID/ULID یا راهکار غیرقابل حدس مشابه استفاده شود.

• شناسه توسط فروشنده قابل شخصی‌سازی نیست.

## ۴.۱۶ زبان و بومی‌سازی

• Market، Product Detail و مسیر تماس در فارسی و انگلیسی در دسترس هستند؛ Admin Panel، Dashboard و پیام‌های مدیریتی فقط فارسی‌اند.

• نسخه فارسی RTL و نسخه انگلیسی LTR است. تغییر زبان باید جهت چیدمان، آیکن‌های جهت‌دار، اعداد و قالب تاریخ را نیز پوشش دهد.

• سوییچر زبان باید با متن «فارسی / English» قابل تشخیص باشد؛ پرچم به‌تنهایی نماینده زبان نباشد.

• زبان در URL ثبت شود. تغییر زبان باید Product جاری، Filterهای معتبر، Sort و Page را حفظ کند؛ Query جستجو خودکار ترجمه نشود.

• Reference Data دارای Label فارسی و انگلیسی است. عنوان و Description محصول ساختار دو‌زبانه دارند و ترجمه آن‌ها توسط فروشنده وارد می‌شود؛ ترجمه خودکار جزو V1 نیست.

• Search ساده روی مقادیر فارسی و انگلیسی موجود کار می‌کند، بدون Semantic Translation.

• نسخه انگلیسی همان فروشگاه تهران و همان مدل تماس/خرید حضوری را معرفی می‌کند و به معنی ارسال بین‌المللی یا پرداخت ارزی نیست.

• شماره تماس در انگلیسی با کد کشور و جهت LTR نمایش داده شود؛ نشانی انگلیسی در صورت وجود داده آماده نمایش داده شود.

• معیار پذیرش: تغییر زبان در Product Detail همان محصول را در زبان دیگر باز کند؛ Filter/Sort/Page از بین نروند؛ Labelها، خطاها و وضعیت موجودی بومی‌سازی شوند.

## ۴.۱۷ تبدیل خودکار روزانه قیمت به دلار

• فروشنده فقط قیمت پایه هر تخته را به تومان وارد و ویرایش می‌کند؛ قیمت USD محاسبه‌شده است و فیلد مستقل قابل ویرایش نیست.

• نرخ مرجع «نرخ فروش دلار بازار آزاد» به صورت «تومان برای یک دلار آمریکا» ذخیره شود و از تأمین‌کننده‌ای که تیم فنی پیش از اتصال واقعی سرویس انتخاب می‌کند دریافت شود. هنگام خطا نوع نرخ نباید به نرخ رسمی یا نرخ خرید تغییر کند؛ اگر منبع ریال برگرداند، پیش از محاسبه به تومان نرمال شود.

• فرمول: price_usd = price_toman / usd_rate_toman. نرخ صفر، منفی، فاقد واحد یا نامعتبر قابل استفاده نیست. نتیجه نمایشی حداکثر تا دو رقم اعشار گرد شود، قیمت پایه تغییر نکند و مبلغ دلاری ماهیت تقریبی داشته باشد.

• به‌روزرسانی حداقل روزانه یک‌بار در زمان قابل پیکربندی با منطقه زمانی تهران انجام شود. زمان اجرای Job، زمان دریافت موفق و زمان نرخ اعلامی منبع از هم تفکیک شوند.

• کارت، Product Detail، Sort و Filter قیمت در یک لحظه باید از یک نسخه مشترک نرخ استفاده کنند. کلید یا Credential تأمین‌کننده نرخ نباید در Frontend قرار گیرد.

• در فارسی Filter قیمت با تومان و در انگلیسی با USD نمایش داده شود؛ State اقتصادی Filter در URL به واحد پایه تومان ذخیره شود.

• اگر Refresh نرخ شکست خورد، آخرین نرخ معتبر همراه Timestamp واقعی آن حفظ شود و زمان نرخ با تلاش ناموفق تازه نشود. وضعیت نرخ قدیمی باید در UX قابل تشخیص باشد؛ پیشنهاد طراحی این است که پس از عبور از آستانه تازگی، برچسب «نرخ به‌روز نیست» نمایش داده شود.

• اگر هیچ نرخ معتبر اولیه وجود ندارد، مبلغ USD ساختگی یا صفر نشان داده نشود؛ قیمت پایه با Label روشن Toman و پیام انگلیسی عدم دسترسی موقت به تبدیل نمایش داده شود.

• در Admin فارسی یک بخش Read-only «وضعیت نرخ دلار» شامل تأمین‌کننده، نوع نرخ (فروش بازار آزاد)، مقدار، زمان نرخ و آخرین دریافت موفق/ناموفق نمایش داده شود؛ ویرایش دستی نرخ توسط فروشنده جزو V1 نیست.

• مبلغ USD باید با واحد صریح USD و Copy «قیمت تقریبی بر اساس نرخ تبدیل» نمایش داده شود. زمان نرخ و امکان مشاهده منبع/تأمین‌کننده در جزئیات فراهم باشد. انتخاب تأمین‌کننده نرخ یک تصمیم فنی پیش از اتصال واقعی سرویس است و مانع ادامه طراحی نیست.

۵. فلوهای تجربه کاربر (UX Flow)

۵.۱ فلو مشتری هدف‌دار

| Market | ←   | جستجو | ←   | فیلتر / مرتب‌سازی | ←   | انتخاب نتیجه | ←   | جزئیات محصول | ←   | تماس |
|--------|-----|-------|-----|------------------|-----|--------------|-----|--------------|-----|------|

۵.۲ فلو مشتری اکتشافی

| Market | ←   | مرور Listing | ←   | انتخاب محصول | ←   | جزئیات محصول | ←   | تماس |
|--------|-----|--------------|-----|--------------|-----|--------------|-----|------|

۵.۳ فلو ثبت محصول

| ایجاد محصول | ←   | نوع فرش | ←   | اطلاعات محصول | ←   | تصاویر + Cover | ←   | اعتبارسنجی | ←   | انتشار |
|-------------|-----|---------|-----|---------------|-----|----------------|-----|------------|-----|--------|

۵.۴ فلو ویرایش و تغییر وضعیت

| لیست محصولات | ←   | انتخاب محصول | ←   | ویرایش / وضعیت | ←   | ذخیره | ←   | Market به‌روز |
|--------------|-----|--------------|-----|----------------|-----|-------|-----|--------------|

۵.۵ جهت طراحی پس از Review Product Designer

• Product Card بر تصویر کامل فرش، عنوان، قیمت، طول × عرض، شهر بافت/تولید و وضعیت رزرو تکیه کند؛ اطلاعات تخصصی کامل در Detail باشد.

• Listing در مبنای V1 با Pagination طراحی شود؛ Back/Refresh باید State و Page را بازیابی کند.

• Price Range دارای ورودی Min/Max با واحد واضح باشد؛ Slider در صورت استفاده مکمل است، نه تنها روش ورود.

• Market بدون Landing/Banner بزرگ مستقیماً با Search، Filter و Listing آغاز شود؛ تمرکز دستباف در Hierarchy بصری اعمال شود، نه با مخفی‌کردن ماشینی.

• پنل تماس نام فروشگاه، شهر، شماره کامل و آدرس در صورت وجود را نمایش دهد؛ محل دقیق نمایش در Detail با Designer است.

• CTA تماس دو مرحله قابل تفکیک داشته باشد: contact_clicked سپس phone_call_clicked؛ Pattern دقیق Modal/Bottom Sheet/Inline با Designer است.

• Dashboard موجودی جاری را از Metrics زمانی جدا کند؛ بازه پیش‌فرض ۳۰ روز، گزینه ۷ روز و بازه دلخواه داشته باشد.

• در موبایل تصویر، عنوان، قیمت، وضعیت، مشخصات اصلی و Contact اولویت داشته باشند؛ در دسکتاپ خلاصه مشخصات/تماس کنار Gallery قابل ارائه است. کنترل اصلی نباید با Responsive/Zoom پوشانده شود.

| Product Designer در صورت نیاز می‌تواند پیشنهاد تغییر Flow یا Requirement بدهد، اما هر تغییر باید قبل از نهایی شدن Design در PRD نیز منعکس شود تا Design و PRD منطبق بمانند. |
|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

۶. نیازمندی‌های غیرعملکردی (Non-Functional Requirements)

۶.۱ Responsive و Mobile Experience

Market و Admin Panel باید روی موبایل و دسکتاپ Responsive باشند. تجربه موبایل برای Market اهمیت بالایی دارد و هیچ Feature اصلی نباید فقط در Desktop قابل استفاده باشد.

۶.۲ Performance

• تصاویر در Listing و Product Detail باید بهینه نمایش داده شوند و فایل اصلی تا ۱۵MB نباید باعث تجربه کند یا غیرقابل استفاده شود.

• Lazy loading / thumbnailing / compression یا راهکار مشابه می‌تواند توسط تیم فنی انتخاب شود.

• Search، Filter و Sort باید بدون Block شدن طولانی UI و با بازخورد مناسب انجام شوند.

• SLO عددی Performance در این PRD تعیین نشده و در صورت نیاز پیش از Release توسط تیم محصول و فنی مشخص می‌شود.

۶.۳ SEO

• Public Product Detail باید URL مستقل و قابل Index شدن توسط موتورهای جستجو داشته باشد.

• محتوای اصلی محصول مانند عنوان، قیمت و مشخصات باید برای Crawler قابل دسترسی باشد.

• محصول Sold تا زمانی که Soft Delete نشده است می‌تواند URL عمومی خود را حفظ کند.

• جزئیات فنی Rendering/SSR/Metadata به تیم فنی واگذار می‌شود، مشروط به دستیابی به Indexability مناسب.

۶.۴ Security

• رمز عبور Admin باید به شکل امن ذخیره شود و هیچ Password خامی نگه‌داری نشود.

• Admin APIs باید Authentication و Authorization مناسب داشته باشند.

• Public Identifier محصول نباید به آسانی قابل حدس از روی ID داخلی باشد.

• Session و Cookieهای Admin باید با تنظیمات امنیتی مناسب پیاده‌سازی شوند.

• ورودی‌های متنی و فایل‌ها باید Validation شوند.

۶.۵ Privacy و Analytics

برای Analytics مشتری از Anonymous ID / Session استفاده می‌شود. در V1 برای تحلیل Funnel نام، شماره موبایل یا سایر PII مشتری جمع‌آوری نمی‌شود.

## ۶.۶ وضعیت‌های رابط و دسترس‌پذیری

• در بخش عمومی هر دو زبان حالت‌های Loading، Empty Market، No Result، Image Error، Data Fetch Error + Retry، Sold و Deleted/Unavailable طراحی شوند.

• در Admin فارسی حالت‌های Login نامعتبر، Form Validation، Upload نامعتبر، Save Success/Failure و Unsaved Changes طراحی شوند.

• برای تبدیل ارز سه وضعیت نرخ معتبر، نرخ قدیمی و نبود هیچ نرخ معتبر باید پوشش داده شود.

• کنترل‌ها با Keyboard قابل استفاده باشند، Focus واضح باشد، Modal/Panel مدیریت Focus صحیح داشته باشد و Status فقط با Color منتقل نشود.

• تصاویر Alt Text داشته باشند و بزرگ‌نمایی متن یا نمایش موبایل باعث پوشانده‌شدن CTA اصلی نشود. ادعای تطابق با استاندارد دسترس‌پذیری فقط پس از آزمون مجاز است.

۷. نیازمندی‌های محیطی و سیستمی

• محصول Web-based است؛ Native Mobile App در V1 نداریم.

• Market و Admin Panel دو Entry Point / Domain مستقل دارند. نام دقیق Domain یا Subdomain در اجرا مشخص می‌شود.

• هر دو بخش باید در مرورگرهای مدرن موبایل و دسکتاپ قابل استفاده باشند.

• Backend به Database برای محصولات، Reference Data، فروشگاه، Admin و Analytics و به Storage مناسب برای تصاویر نیاز دارد.

• Backend به سرویس زمان‌بندی‌شده برای دریافت روزانه نرخ USD، نگه‌داری آخرین نرخ معتبر و مدیریت خطا نیاز دارد؛ دریافت نرخ واقعی در Client انجام نمی‌شود.

• زیرساخت باید امکان توسعه به Multi-vendor در V2 را بدون وابستگی محصولی V1 به یک مدل غیرقابل توسعه حفظ کند؛ تصمیم معماری با تیم فنی است.

۸. فرضیات، محدودیت‌ها و ریسک‌ها

۸.۱ فرضیات

• مشتری پیش از خرید نهایی مایل است تصاویر، مشخصات و قیمت را آنلاین ببیند.

• مشاهده حضوری در این بازار بخشی از تصمیم نهایی خرید باقی می‌ماند.

• فروشنده اطلاعات محصول، قیمت و وضعیت موجودی را به‌موقع به‌روزرسانی می‌کند.

• قیمت نمایش‌داده‌شده یکی از عناصر اصلی تصمیم‌گیری اولیه مشتری است.

• کیفیت داده‌های ساختاریافته برای Search و Filter مناسب خواهد بود.

• فروشنده محتوای محصول را به فارسی و انگلیسی وارد می‌کند و تیم محصول Reference Data دو‌زبانه اولیه را آماده می‌کند.

۸.۲ محدودیت‌ها

• در V1 تنها یک فروشگاه و یک Admin وجود دارد.

• خرید و فروش نهایی خارج از سیستم انجام می‌شود.

• سیستم نمی‌تواند با قطعیت تماس موفق یا فروش حضوری را اندازه‌گیری کند.

• Reference Data در V1 توسط فروشنده قابل مدیریت نیست.

• ویدئو و بیش از ۱۰ تصویر پشتیبانی نمی‌شود.

۸.۳ ریسک‌ها

| ریسک                            | اثر                                                             | کاهش ریسک                                                                                         |
|---------------------------------|-----------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| قدیمی شدن قیمت یا وضعیت موجودی  | اعتماد کاربر کاهش می‌یابد یا تماس‌های نامرتبط ایجاد می‌شود.        | Workflow و UI باید Update وضعیت/قیمت را برای Admin ساده کند.                                      |
| کیفیت پایین تصاویر              | ارزیابی اولیه مشتری سخت می‌شود و Contact Conversion کاهش می‌یابد. | راهنمای Upload و نمایش واضح Cover/Gallery در Design.                                              |
| Taxonomy نامناسب Reference Data | Filterها ارزش خود را از دست می‌دهند.                             | Reference Data اولیه با دانش بازار آماده و در نسخه‌های بعد قابل مدیریت شود.                        |
| عدم مشاهده فروش نهایی           | KPI ممکن است فقط Intent را اندازه بگیرد نه Revenue.             | Contact Conversion به‌عنوان Proxy شفاف تعریف شود و ادعای فروش نهایی نشود.                          |
| بار تصاویر حجیم                 | Performance موبایل کاهش پیدا می‌کند.                             | Image optimization و محدودیت ۱۵MB برای Upload.                                                    |
| قدیمی یا نامعتبر شدن نرخ ارز    | قیمت USD می‌تواند گمراه‌کننده یا ناسازگار شود.                    | ذخیره آخرین نرخ معتبر، Timestamp واقعی، حالت Stale/Unavailable و یک Rate Version مشترک در هر نما. |
| ناهماهنگی ترجمه فارسی و انگلیسی | اعتماد کاربر و کیفیت Search/SEO نسخه انگلیسی کاهش می‌یابد.       | ورودی دو‌زبانه ساختاریافته، Reference Data دو‌زبانه و Validation/بازبینی محتوا.                     |

۹. وابستگی‌ها

• آماده بودن Reference Data اولیه برای نوع بافت، طرح، جنس، رنگ، شهر و سایر گزینه‌های محدود.

• تهیه اطلاعات و تصاویر اولیه محصولات فروشگاه فرش ایران.

• زیرساخت ذخیره‌سازی و Delivery تصاویر.

• زیرساخت ثبت و نگه‌داری Eventهای Analytics.

• آماده بودن اطلاعات هویتی فروشگاه شامل نام، شهر و شماره موبایل.

• هم‌راستا بودن Data Model با نیازهای Search/Filter و توسعه Multi-vendor آینده.

در V1 وابستگی به Payment Gateway، سرویس پیامک OTP یا سرویس Messaging وجود ندارد.

• تأمین‌کننده نرخ فروش دلار بازار آزاد با دسترسی مجاز، واحد مشخص و Timestamp نرخ. انتخاب Provider به تیم فنی واگذار شده و باید پیش از اتصال واقعی سرویس انجام شود.

• Job زمان‌بندی‌شده برای دریافت روزانه نرخ، نگه‌داری آخرین نرخ معتبر و ثبت وضعیت دریافت موفق/ناموفق.

• Reference Data فارسی/انگلیسی و محتوای نمونه دو‌زبانه برای طراحی و تست.

۱۰. وضعیت کنونی محصول

محصول در زمان نگارش این سند از صفر در حال توسعه است و نسخه عملیاتی قبلی وجود ندارد. V1 به‌عنوان Pilot با یک فروشگاه طراحی می‌شود تا نیاز بازار، رفتار Discovery و Contact Conversion اندازه‌گیری شود.

۱۱. برنامه انتشار کنونی — V1

<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr class="header">
<th>Epic</th>
<th>Stories / Scope</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Epic 1 — Market</td>
<td>Listing عمومی دو‌زبانه<br />
Search فارسی/انگلیسی<br />
Filter چندانتخابی با منطق OR/AND<br />
Pagination<br />
Sort با پیش‌فرض جدیدترین<br />
Query Params برای Search/Filter/Sort/Page</td>
</tr>
<tr class="even">
<td>Epic 2 — Product Detail</td>
<td>Gallery و Cover<br />
قیمت و مشخصات دو‌زبانه<br />
وضعیت موجودی<br />
CTA تماس دو مرحله‌ای<br />
Public URL مشترک بین زبان‌ها</td>
</tr>
<tr class="odd">
<td>Epic 3 — Admin Authentication</td>
<td>Login با موبایل و رمز عبور<br />
Logout</td>
</tr>
<tr class="even">
<td>Epic 4 — Product Management</td>
<td>Product List با Search/Filter<br />
Create/Edit محتوای فارسی و انگلیسی<br />
Dynamic Form دستباف/ماشینی<br />
Multi-select جنس/رنگ<br />
Image Upload<br />
Status Management<br />
Soft Delete</td>
</tr>
<tr class="odd">
<td>Epic 5 — Dashboard &amp; Analytics</td>
<td>Event Tracking با language property<br />
Contact Conversion Rate بر اساس Session یکتا<br />
Product Views / Contact Intent / Phone Call Clicks<br />
Top Products<br />
Search Queries</td>
</tr>
<tr class="even">
<td>Epic 6 — Localization &amp; USD</td>
<td>Market فارسی/انگلیسی<br />
Reference Data دو‌زبانه<br />
ورودی محتوای دو‌زبانه در Admin<br />
تبدیل خودکار روزانه USD بر اساس نرخ فروش دلار بازار آزاد<br />
نمایش قیمت تقریبی USD<br />
وضعیت نرخ معتبر/قدیمی/ناموجود<br />
نمایش Read-only وضعیت نرخ در Admin</td>
</tr>
<tr class="odd">
<td>Epic 7 — Non-functional</td>
<td>Responsive<br />
SEO دو‌زبانه<br />
Security<br />
Performance و Image Optimization<br />
Accessibility / UI States</td>
</tr>
</tbody>
</table>

۱۱.۱ Definition of Done نسخه ۱

• کاربر عمومی بتواند بدون Login محصولات را پیدا، فیلتر، مرتب و مشاهده کند.

• اطلاعات و قیمت محصول پیش از تماس در دسترس باشد.

• تماس با فروشنده در هر سه وضعیت Available/Reserved/Sold امکان‌پذیر باشد.

• Admin بتواند با موبایل و رمز وارد شود و چرخه کامل مدیریت محصول را انجام دهد.

• Analytics Funnel و Dashboard حداقل برای KPIهای تعریف‌شده داده معتبر تولید کنند.

• Market و Admin روی موبایل و دسکتاپ قابل استفاده باشند.

• Design نهایی و PRD از نظر Flow و Requirement با یکدیگر منطبق باشند.

• Market و Product Detail در فارسی و انگلیسی قابل استفاده باشند و تغییر زبان State معتبر را حفظ کند.

• هر محصول یک تخته مستطیلی با طول/عرض جداگانه باشد؛ جنس و رنگ چندانتخابی باشند و فرم تخصصی نوع فرش درست عمل کند.

• نمایش USD از قیمت پایه تومانی و آخرین نرخ معتبر «فروش دلار بازار آزاد» محاسبه شود؛ مبلغ به‌صورت تقریبی نمایش داده شود و Rate مشترک بین Card/Detail/Filter/Sort حفظ شود.

• حالات نرخ معتبر، قدیمی و ناموجود بدون نمایش مبلغ ساختگی پوشش داده شوند.

• Contact Conversion Rate طبق تعریف Session یکتا محاسبه شود و Raw Clickها از Unique Session Metrics تفکیک شوند.

• حالت‌های Loading/Empty/Error و الزامات اصلی Keyboard/Focus/Alt Text در طراحی و پیاده‌سازی پوشش داده شوند.

۱۲. برنامه انتشار آینده

۱۲.۱ Version 2 — Multi-vendor SaaS & Subscription

• تبدیل محصول از یک فروشگاه به چند فروشنده/فروشگاه.

• ثبت‌نام فروشنده و ایجاد Store.

• Super Admin برای بررسی و فعال/غیرفعال کردن فروشگاه‌ها.

• فروش Subscription با مدت اعتبار و وضعیت فعال/منقضی.

• هر فروشنده محصولات را با نام فروشگاه و اطلاعات تماس خود منتشر کند.

• مدیریت اعضای فروشگاه و دسترسی چند کاربر به Admin Panel.

• Landing Page برای هدایت به Market یا پنل مدیریت.

• مدیریت بخشی از Reference Data توسط Super Admin در صورت نیاز.

۱۲.۲ Version 3 — Messaging مشتری و فروشنده

• کاربر بتواند درباره یک محصول مشخص به فروشنده پیام ارسال کند.

• فروشنده Conversationها را داخل Admin Panel مشاهده و پاسخ دهد.

• Conversation به Product مربوطه متصل باشد تا Context حفظ شود.

• روش دقیق شناسایی/Authentication مشتری و Notificationها در PRD نسخه ۳ نهایی می‌شود.

۱۳. استراتژی فروش و ورود به بازار

V1 یک Pilot تک‌فروشنده با فروشگاه فرش ایران است و هدف اصلی آن اعتبارسنجی رفتار استفاده و ایجاد Baseline برای KPIهاست، نه فروش Subscription یا جذب گسترده فروشنده.

• عرضه اولیه با موجودی واقعی فروشگاه و اطلاعات کامل و قیمت‌های به‌روز انجام شود.

• SEO به‌عنوان قابلیت زیرساختی جذب Organic Traffic در V1 در نظر گرفته شده است.

• کانال‌های بازاریابی پولی، بودجه Acquisition و برنامه فروش سازمانی در این PRD تعیین نشده و در صورت نیاز با تیم Marketing/Sales تدوین می‌شود.

• تصمیم Go/No-Go برای توسعه مقیاس فروشندگان در V2 باید با داده‌های استفاده، Contact Conversion و بازخورد فروشنده پایلوت پشتیبانی شود.

۱۴. پیش‌بینی درآمد

در V1 مدل درآمدی پلتفرم تعریف نشده است و محصول با یک فروشگاه به‌عنوان Pilot اجرا می‌شود. مدل Monetization پیشنهادی در V2 فروش Subscription به فروشندگان است. قیمت‌گذاری پلن‌ها و پیش‌بینی درآمد نیازمند PRD و تحلیل جداگانه با تیم فروش است و در این سند عددی برای آن تعیین نمی‌شود.

۱۵. واژه‌نامه، اصطلاحات و عبارات اختصاری

| اصطلاح                  | تعریف                                                                                                      |
|-------------------------|------------------------------------------------------------------------------------------------------------|
| Market                  | بخش عمومی محصول برای مشاهده و پیدا کردن فرش‌ها.                                                             |
| Admin                   | مدیر فروشگاه که در V1 تنها یک حساب برای فروشگاه دارد.                                                      |
| Product Detail          | صفحه عمومی جزئیات یک فرش.                                                                                  |
| Reference Data          | گزینه‌های از پیش تعریف‌شده مانند طرح، رنگ، جنس و نوع بافت.                                                   |
| Available               | محصول موجود و قابل نمایش در Listing.                                                                       |
| Reserved                | محصول رزروشده که همچنان در Listing نمایش داده می‌شود.                                                       |
| Sold                    | محصول فروخته‌شده؛ از Listing پیش‌فرض حذف می‌شود ولی URL و Detail باقی می‌ماند.                                 |
| Soft Delete             | حذف منطقی بدون پاک کردن دائمی رکورد از Database.                                                           |
| Contact Conversion Rate | نسبت قصد تماس به مشاهده Product Detail.                                                                    |
| Anonymous ID / Session  | شناسه غیرهویتی برای تحلیل رفتار کاربر بدون Account.                                                        |
| Public ID               | شناسه غیرقابل حدس محصول برای URL/API عمومی.                                                                |
| V1                      | نسخه اول تک‌فروشنده.                                                                                        |
| V2                      | نسخه چندفروشنده با Subscription.                                                                           |
| V3                      | نسخه Messaging مشتری و فروشنده.                                                                            |
| Localization            | پشتیبانی از فارسی RTL و انگلیسی LTR در بخش عمومی، با حفظ State و URL مناسب زبان.                           |
| USD Rate                | نرخ فروش دلار بازار آزاد به واحد «تومان برای یک دلار آمریکا» که مبنای محاسبه قیمت تقریبی نسخه انگلیسی است. |
| Rate Stale              | آخرین نرخ معتبر موجود است اما از آستانه تازگی تعریف‌شده عبور کرده و باید به‌عنوان نرخ قدیمی نمایش داده شود.  |

۱۶. پیوست

۱۶.۱ خلاصه تصمیم‌های قطعی برای Product Designer

• Landing Page در V1 نداریم؛ Market و Admin دو Entry Point مستقل‌اند.

• کاربر Market هیچ Login/Account ندارد.

• قیمت همه محصولات اجباری است.

• تمام فیلدهای عمومی محصول اجباری‌اند و Description اختیاری است.

• فرم محصول برای دستباف و ماشینی Dynamic است.

• حداقل ۱ و حداکثر ۱۰ تصویر و یک Cover؛ هر فایل حداکثر ۱۵MB.

• Reserved در Listing می‌ماند؛ Sold از Listing پیش‌فرض حذف می‌شود ولی Detail باقی می‌ماند.

• تماس با فروشنده همیشه فعال است، حتی برای Sold.

• Search ساده، Filter قوی و Multi-select؛ State در Query Params.

• Sort قابل تغییر با پیش‌فرض جدیدترین.

• Draft، Discount، Share، Similar Products، Video و Wishlist در V1 نداریم.

• Admin با Mobile + Password وارد می‌شود و OTP نداریم.

• حذف محصول Soft Delete است.

• Analytics و Dashboard بخشی از Scope V1 هستند.

• بخش عمومی دو‌زبانه فارسی/انگلیسی و Admin فقط فارسی است؛ فروشنده محتوای دو‌زبانه محصول را وارد می‌کند.

• اولویت محتوایی/بصری با دستباف است، اما ماشینی در V1 باقی می‌ماند.

• هر Product یک تخته فرش است؛ فقط مستطیل با طول/عرض جداگانه پشتیبانی می‌شود.

• جنس و رنگ Multi-select هستند.

• نسخه انگلیسی قیمت تقریبی USD را با تبدیل خودکار روزانه از قیمت پایه تومان و بر اساس نرخ فروش دلار بازار آزاد نمایش می‌دهد؛ انتخاب تأمین‌کننده نرخ بر عهده تیم فنی پیش از اتصال سرویس است.

• KPI Contact Conversion Rate بر مبنای Session یکتا محاسبه می‌شود.

• Listing مبنای طراحی Pagination دارد و Contact Flow دو مرحله Analytics-friendly است.

۱۶.۲ تصمیم غیرمسدودکننده باقی‌مانده

• L1 — سیاست انتشار محتوای انگلیسی: فروشنده محتوای دو‌زبانه را وارد می‌کند، اما هنوز باید نهایی شود آیا عنوان انگلیسی برای Publish اجباری است و در نبود Description انگلیسی، متن فارسی با برچسب زبان اصلی نمایش داده شود یا نه. این مورد مانع شروع طراحی بصری نیست.

## ۱۶.۳ قرارداد تحویل طراحی

• تحویل طراحی V1 یک فایل HTML کاملاً آفلاین و تک‌فایلی با CSS، JavaScript، فونت، آیکن و تصاویر لازم داخلی باشد.

• نمونه باید Market، Product Detail، Login Admin، Dashboard، Product List و Create/Edit Product را با داده نمونه قابل تست پوشش دهد.

• Login، ذخیره محصول و Analytics در Prototype شبیه‌سازی می‌شوند و نباید به‌عنوان Authentication یا Persistence امن محصول واقعی تلقی شوند.

• تغییرات نمایشی Admin در Prototype در همان نمونه روی Market اثر بگذارند؛ Local persistence در صورت استفاده صرفاً رفتار نمایشی است.

• نرخ USD در فایل آفلاین با داده نمونه و Label واضح «شبیه‌سازی نرخ؛ داده روز نیست» ارائه شود و حالت معتبر/قدیمی/ناموجود قابل تست باشد.

• Prototype تک‌فایلی جایگزین معماری واقعی دو Entry Point مستقل Market/Admin، SEO، Security، Storage و سرویس Rate آنلاین نیست.

• جهت بصری پیشنهادی: گالری معاصر با نشانه‌های محدود از اصالت فرش ایرانی؛ تصویر فرش محور باشد و تزئینات با نقش خود فرش رقابت نکند.

| وضعیت سند: Draft v0.3. بازبینی سوم طراحی ادغام شده است. مبنای نرخ USD «نرخ فروش دلار بازار آزاد» و ماهیت تقریبی مبلغ دلاری قطعی شده؛ انتخاب تأمین‌کننده نرخ بر عهده تیم فنی پیش از اتصال واقعی سرویس است. مورد L1 در پیوست ۱۶.۲ مانع شروع طراحی بصری نیست. هر تغییر بعدی Requirement یا Flow باید همزمان در PRD و Design اعمال شود. |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
